from Model import model_compressor, model_turbo

#Uncomment the below lines for testing
# model_compressor and model_turbo takes one argument i.e path

model = model_compressor("D://Data/propulsion.csv")
#model.predict(X)

#model1 = model_turbo("D://Data/propulsion.csv")
#model1.predict(X)
